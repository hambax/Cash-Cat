-- Health insurance (distinct from general insurance) — Southern Cross, etc.

INSERT OR IGNORE INTO categories(key, display_name, source, sort_order, archived) VALUES
  ('health_insurance', 'Health insurance', 'preset', 37, 0);
